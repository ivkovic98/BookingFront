export type Manager = {
  id: number;
  name: string;
  email: string;
};

export type RegisterManagerModel = {
  name: string;
  surname: string;
  email: string;
  password: string;
  phoneNumber: string;
  hotelId: string;
};
